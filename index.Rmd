--- 
title: "POL342 R Tutorials"
date: "`r Sys.Date()`"
site: bookdown::bookdown_site
output: bookdown::gitbook
documentclass: book
bibliography: [book.bib, packages.bib]
biblio-style: apalike
link-citations: yes
github-repo: rstudio/bookdown-demo
description: "Tutorial notes for UTM POL342"
---

# Welcome {-}

Tutorial notes for POL342. New chapters are added as the term progresses.

```{r include=FALSE}

# Stop help pages (e.g. ?read.csv) from opening in the browser during the build
options(browser = function(url) invisible(NULL))

#Install bookdown package and tinytex to run compilation
#require("bookdown")
#require("tinytex")

#To compile, go to the "Build" tab > "Build Book"
```



```{r include=FALSE}
# automatically create a bib database for R packages
knitr::write_bib(c(
  .packages(), 'bookdown', 'knitr', 'rmarkdown'
), 'packages.bib')
```
